server {
    listen 3000;
    server_name _;

    # Serve Nuxt generated static site
    root /usr/share/nginx/html;
    index index.html;

    # Try static files first, then fallback to Nuxt server endpoints
    location / {
        try_files $uri $uri/ /index.html;
    }
}


